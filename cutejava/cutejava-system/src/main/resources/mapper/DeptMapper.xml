<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.odboy.modules.system.mapper.DeptMapper">
    <select id="findAll" resultType="cn.odboy.model.system.domain.Dept">
        select
        *
        from sys_dept
        <where>
            <if test="criteria.ids != null and criteria.ids.size() > 0">
                and dept_id in
                <foreach collection="criteria.ids" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="criteria.name != null and criteria.name != ''">
                and name like concat('%', #{criteria.name}, '%')
            </if>
            <if test="criteria.enabled != null">
                and enabled = #{criteria.enabled}
            </if>
            <if test="criteria.pid != null">
                and pid = #{criteria.pid}
            </if>
            <if test="criteria.pidIsNull != null">
                and pid is null
            </if>
            <if test="criteria.createTime != null and criteria.createTime.size() != 0">
                and create_time between #{criteria.createTime[0]} and #{criteria.createTime[1]}
            </if>
        </where>
        order by dept_sort
    </select>

    <select id="findByPid" resultType="cn.odboy.model.system.domain.Dept">
        select
        *
        from sys_dept
        where pid = #{pid}
    </select>

    <select id="findByPidIsNull" resultType="cn.odboy.model.system.domain.Dept">
        select
       *
        from sys_dept
        where pid is null
    </select>

    <select id="findByRoleId" resultType="cn.odboy.model.system.domain.Dept">
        select d.dept_id as id,
               d.name,
               d.pid,
               d.sub_count,
               d.create_time,
               d.update_time,
               d.create_by,
               d.update_by,
               d.enabled,
               d.dept_sort
        from sys_dept d,
             sys_roles_depts r
        where d.dept_id = r.dept_id
          and r.role_id = #{roleId}
    </select>
</mapper>